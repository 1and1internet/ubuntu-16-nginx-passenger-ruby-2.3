Passenger document root: /var/www/public

# An example using Sinatra is provided by default

# Example Rails setup

ssh-user:~$ gem install rails
ssh-user:~$ rails new .
ssh-user:~$ touch tmp/restart.txt
